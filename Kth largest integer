class Solution:
    def kthLargestNumber(self, nums: List[str], k: int) -> str:
        int_array = []
        for i in range(len(nums)):
            int_array.append(int(nums[i]))
        int_array.sort()
        return str(int_array[-1*k])
